<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.siberiadante.utilsample">

    <uses-permission
        android:name="android.permission.DELETE_PACKAGES"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.INSTALL_PACKAGES" />

    <application
        android:name=".UtilSampleApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.siberiadante.utilsample.fileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondActivity" />
        <activity android:name=".activity.SDActivityUtilActivity" />
        <!-- <activity android:name=".util.AppActivity" /> -->
        <!-- <activity android:name=".util.BitmapUtilActivity" /> -->
        <!-- <activity android:name=".util.DateUtilActivity" /> -->
        <!-- <activity android:name=".util.EncryptionActivity" /> -->
        <!-- <activity android:name=".util.NetworkActivity" /> -->
        <!-- <activity android:name=".util.SDCardUtilActivity" /> -->
        <activity android:name=".activity.SDJumpUtilActivity" />
        <activity android:name=".activity.SDTimerUtilActivity" />
        <activity android:name=".activity.SDTransitionUtilActivity" />
        <activity android:name=".activity.SDCloseUtilActivity" />
        <activity android:name=".activity.SDAppUtilActivity" />
        <activity android:name=".activity.SDDateUtilActivity" />
        <activity android:name=".activity.SDNumberUtilActivity" />
        <activity android:name=".activity.SDRegexUtilActivity" />
        <activity android:name=".activity.SDLogUtilActivity" />
        <activity android:name=".activity.SDMathActivity"></activity>
    </application>

</manifest>