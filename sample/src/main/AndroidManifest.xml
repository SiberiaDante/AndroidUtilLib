<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sample">

    <application
        android:name=".SampleApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.PhotoViewActivity" />
        <activity android:name=".ui.activity.Main2Activity" />
        <activity android:name=".ui.activity.ToastActivity" />
        <activity android:name=".ui.activity.NetworkActivity" />
        <activity android:name=".ui.activity.BaseActivity" />
        <activity android:name=".ui.activity.ActivityUtilActivity" />
        <activity
            android:name=".ui.activity.ScreenActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".ui.activity.AppActivity" />

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.sample.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <activity android:name=".ui.activity.ClearActivity" />
        <activity android:name=".ui.activity.DialogActivity" />
        <activity android:name=".ui.activity.NumberActivity" />
        <activity android:name=".ui.activity.WebViewActivity" />
        <activity android:name=".ui.activity.QQExercisePedometerActivity"></activity>
    </application>

</manifest>